import React from "react";

import Layout from "../components/Layout";

const RemindersPage = () => (
  <Layout>
    <h1>Напоминания</h1>
    {/* TODO: list reminders + channel toggles */}
    <p>Скоро тут появится настройка контекстных триггеров</p>
  </Layout>
);

export default RemindersPage;
