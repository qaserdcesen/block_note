# Web (React)

Placeholder React/Vite-ready structure:
- `src/pages` — index/tasks/habits/reminders/settings.
- `src/components` — Layout plus task/habit lists.

TODO: bootstrap a Vite/Next/CRA project, drop these files in `src/`, and connect to the FastAPI backend with `fetch` or `axios`.
